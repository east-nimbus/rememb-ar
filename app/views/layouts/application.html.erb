<!DOCTYPE html>
<html>
  <head>
    <title>rememb-ar</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body data-theme="luxury" class="flex flex-col h-screen overflow-hidden">
    <%= render 'shared/header' %>
    <main class="flex-grow flex flex-col w-full overflow-y-auto bg-gray-100">
      <% flash.each do |type, message| %>
        <div class="px-4 pt-4">
          <div class="alert <%= type == 'notice' ? 'alert-info' : 'alert-error' %> shadow-lg text-white">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-6 h-6"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            <span><%= message %></span>
          </div>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
